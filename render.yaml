services:
  - type: web
    name: fraudwatch
    env: python
    buildCommand: pip install -r requirements.txt && python model/train.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
